# [P1.6] Security Review — dep audit, token-leak scan, CSRF/state

> Grounded in `brand-digital-twin` @ `fb03ddd`. State-forgery is already tested
> (`oauth_flows_test.ts`) and the A2.5 ticket has replay + expiry tests
> (`server_test.ts:894–923`). This ticket closes the two *untracked* items and
> formalizes the audit as a recurring gate.

## Build / actions
1. **Dependency audit (recurring):**
   - Triage the `npm audit` advisories CI surfaced (4 high / 1 moderate in the
     client tree; `next` already on patched 14.2.35). Resolve or document each with
     a rationale + expiry date.
   - Add `npm audit --audit-level=high` to `brand-twin-app-ci.yml` —
     warn first sprint, then blocking after triage. Mirror with the engine audit.
2. **Token-leak scan:**
   - One **centralized redaction scrubber** (same one P1.2a mandates) over all log
     + `error_events` writes; allowlist-based, strips bearer/refresh/`Authorization`/
     `*_secret`/`*_key`/PAN patterns.
   - A CI grep gate over source + a sampled log fixture asserting no raw-token
     pattern is ever emitted.
3. **CSRF / state (verify + extend):**
   - Confirm every OAuth callback verifies the signed `state`
     (`{tenantId, platform, nonce}`, short TTL); extend tests to cover
     **cross-tenant state injection**, not just forgery.
   - A2.5 tickets: confirm single-use burn + expiry (tests exist) and that no
     bearer ever rides in a URL.

## Checklist
- [ ] `npm audit` advisories triaged + documented; audit step in CI
- [ ] Centralized redaction scrubber + leak-scan CI gate + unit tests
- [ ] Cross-tenant state-injection test added to `oauth_flows_test`
- [ ] No-token-in-URL assertion for connect + stream

## Done when
State-forgery + cross-tenant tests green, no token in any log/payload, dep
advisories all triaged.
